#!/bin/bash

# Start Flask API in background
python api/app.py &

# Wait a moment for Flask to start
sleep 2

# Start Bot in foreground (this will keep the container alive)
python -m bot.main

# If bot dies, keep container alive by waiting for Flask
wait
